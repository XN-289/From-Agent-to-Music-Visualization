# 项目执行手册：从 Agent 到音乐可视化

> 本手册是**唯一执行入口**：任何设备（新电脑、新会话、新 Agent）克隆仓库后，先读本手册即可知道项目是什么、做到哪了、下一步做什么。
> 更新日期：2026-08-21 · 当前状态：**代码任务 2-8 已完成；Stage 真实验收通过，待 Folia 桌面端与视频导出实测**

## 一、项目是什么（一句话）

**本地闭环：打开对话框 → 与「专业 AI 音乐制作人」Agent 聊天深挖需求 → Suno 真实生成歌曲 → 生成完自动推送到 Folia 可视化播放器并自动播放（原文+中文翻译副字幕+封面）→ 长时间全屏观看 → 导出横屏/竖屏纯净视频发到视频网站。**

用户原话核心：用户是音乐小白；Agent 负责深挖「想要什么表现形式、表达什么情感」；最终目的是**表达欲**——把自己做的歌的视觉效果发到视频网站给大家看。

## 二、仓库与分支

- 远程：`https://github.com/XN-289/From-Agent-to-Music-Visualization.git`（SSH `git@github.com:XN-289/From-Agent-to-Music-Visualization.git` 在本机网络下极不稳定，**一律用 HTTPS**）
- 默认分支：`main`（文档与基线）
- 实现分支：`feat/pipeline-complete-experience`（本机已建；新设备执行时 `git checkout -b feat/pipeline-complete-experience`）
- 克隆命令：`git clone https://github.com/XN-289/From-Agent-to-Music-Visualization.git`
- 结构：`music-agent/`（Next.js 16 + pi Agent，我们自己的代码）、`folia-major/`（上游 chthollyphile/folia-major 副本，**AGPL-3.0**，改动保持最小）、`docs/`（设计/计划/本手册）

## 三、需求决策记录（已与用户逐项确认，不可随意更改）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 风格选取入口 | **纯对话里挖**（不加预设风格卡片） |
| 2 | 歌曲语言 | **中日双语**；日文歌必须有中文翻译副字幕（P0 修复） |
| 3 | 生成产物 | **音频 + 歌词 + 封面** |
| 4 | 推送时机 | **自动推送为主，详情页按钮兜底** |
| 5 | 推送后行为 | **自动播放**（Folia 已内置，`useStagePlaybackController.ts:1195`） |
| 6 | 发布平台 | **横竖都要**（Folia 已内置 1280x720 / 1920x1080 / 1080x1920 三预设） |
| 7 | 曲库主入口 | **Folia 为主**，music-agent 曲库管生成记录 |
| 8 | 验收标准 | **一次性验收**：全部功能做完才算成功 |
| 9 | 封面来源 | 优先使用公司音乐网关返回的生成封面，下载失败或缺失时回退本地渐变+歌名渲染 |
| 10 | 首次测试生成方式 | mock 免费跑通，再切 musicproxy（公司统一网关）真实 |
| 11 | 用户最终只验收两件事 | ① 用户**亲自测试**产品；② 我交付**完整流程文档**供检阅 |

## 四、设计（已批准，冻结）

完整设计：`docs/superpowers/specs/2026-08-21-music-agent-folia-design.md`

核心方案（全部基于代码级侦察验证）：

1. **翻译副字幕链路（P0）**：harness 写词规范让 Agent 在日文歌每行下写 `// 中文翻译` 行 → 提交 Suno 前剥离（`stripTranslationLines`）→ 落盘 `lyrics.t.lrc`（与主歌词共轴）→ 推送前用自定义 ID3v2.3 writer 把主歌词+翻译写入 MP3 的 **USLT 双帧**（原文 `language='eng'`；翻译 `language='chi'` + descriptor `translation`，与 Folia `metadataParser.worker.ts` / `stageApi.cjs` 的识别合同精确对齐）→ Folia 端 3 处最小透传（`translationLyrics` 字段带出 → 渲染端 `tLrcContent` 显示副字幕）。
2. **封面**：优先下载公司音乐网关返回的 `image_url`（常见 jpeg/png/webp），失败回退本地渐变 PNG（@resvg/resvg-js，系统字体中文）；推送走 Stage 原生 `coverFile` 字段 + 嵌入 MP3 APIC 双保险。
3. **自动推送**：已存在（`queueAutoDelivery`，jobs 完成时触发）；进程内去重，失败静默、按钮兜底。
4. **零开发项**：自动播放、横竖屏导出、封面推送通道——全部 Folia 已内置。
5. **mock 限制**：mock 产出 **WAV**（无 ID3），翻译副字幕只能在真实 MP3（musicproxy/公司网关）上验收；mock 阶段只验 t.lrc 落盘与推送不炸。

## 五、实现计划（已批准，冻结）

完整计划：`docs/superpowers/plans/2026-08-21-music-agent-folia-design.md`（1180 行，含每个任务的逐步骤操作、完整代码、测试代码、提交命令）

> 执行偏差：用户 2026-08-21 明确改为「真实生成直接用公司 API/网关，封面一起做」。因此计划文档中的 `sunoapi` 真实验收与「本地渐变封面」历史写法，以本手册 §三、§四的最新结论为准：真实 provider 用 `musicproxy`，封面优先 Provider `image_url`，失败回退本地渐变。

任务总览：

| # | 任务 | 状态 | 关键产出 |
|---|---|---|---|
| 1 | 基线跑通（db:push/双服务/Stage/mock 验证） | ⏸ 部分完成（Stage dev 已跑通，Folia 桌面端待启动） | 可工作的基线环境 |
| 2 | 双语歌词数据模型（lrc.ts + vitest 基建） | ✅ 已完成 | `stripTranslationLines`/`parseLyricPairs`/`buildTranslationLines` + 8 测试 |
| 3 | 落盘与 DB（lyricsTlrc 列 / t.lrc 文件 / 提交剥离） | ✅ 已完成 | 翻译落盘全链路 + 2 测试 |
| 4 | harness 双语写词规范（lyric-writing.md 第十二节） | ✅ 已完成 | Agent 产出 `//` 翻译行 |
| 5 | MP3 USLT 双帧嵌入（自定义 ID3v2.3 writer + 读回验证脚本） | ✅ 已完成并真实验证 | `embedSongMetadata` + `scripts/check-metadata.mjs` |
| 6 | 封面 PNG（@resvg 渲染 + cover-image 路由 + 曲库卡片） | ✅ 已完成 | `renderCoverPng` + GET `/api/songs/[id]/cover-image` + 2 测试 |
| 7 | Folia 翻译透传（stageApi.cjs/types.ts/useStagePlaybackController） | ✅ 已完成并 Stage 验证 | `StageMediaSession.translationLyrics` |
| 8 | 歌词面板副字幕（mergeTranslations + lyrics-panel） | ✅ 已完成 | 主行下方淡色翻译 + 1 测试 |
| 9 | mock 端到端验收（中文歌/日文歌/断 Folia 兜底） | ✅ 已完成 | 免费验证全部链路 |
| 10 | musicproxy 真实验收（日文副字幕对齐/中文/横竖导出/用户测试） | 未开始 | 核心验收 |
| 11 | 流程文档（docs/使用与开发流程.md + README 更新） | ✅ 部分完成 | 用户检阅文档 |

## 六、当前进度快照（截至 2026-08-21 午间）

已完成 ✅：
- 仓库克隆（HTTPS，SSH 不稳）
- 依赖安装：music-agent `pnpm install` ✅、folia-major `npm install` ✅
- `music-agent/.env.local` 已配置（`SUNO_PROVIDER=musicproxy`，公司音乐网关 key 已回填；Stage token 已由 `stage-api-dev.cjs --sync-env` 同步）
- 设计文档、实现计划文档已提交（main 上 2 个 docs commit）
- 实现分支 `feat/pipeline-complete-experience` 已创建
- Task 2-8 的代码、测试与 Folia 最小透传已落地；`music-agent` 测试 3 文件/12 用例通过，`tsc --noEmit`、`lint`（0 error / 3 既有 warning）通过
- 真实公司音乐代理歌曲已推送 Stage：`mediaSession.lyricsText`、`translationLyrics`、`coverUrl` 均返回；封面接口 200 `image/jpeg`
- 修复真实文件名读取 bug：`loadPersistedSong` 的音频正则不再要求 `audio-<index>-<第二个数字段>`，改为匹配 `audio-<index>-<variantId>.<ext>`。
- Task 9 mock 免费链路已完成：中文歌无 `lyrics.t.lrc`，日文歌落盘 `lyrics.t.lrc` 且 `meta.json` 含 `tLrc`；Stage 自动推送成功；关 Stage 后生成仍成功，手动推送 503，重启 Stage 后重推 200。

未完成 / 阻塞 ⏳：
- Task 10 中 Folia 桌面端实际渲染、自动播放、横竖屏视频导出尚未实测；当前只证明 Stage 元数据摄取链路。
- Task 11 文档已更新并补上离线验收方法，但还需要用户在 Folia 桌面端完成最终目视测试后回填验收结论。

## 七、环境与前置（本机实测）

- 硬件：ASUS TUF A15（Ryzen 7 7735H 8C16T / **RTX 4060 Laptop 8GB 显存** / 16GB 内存 / C 盘 88GB 空闲）
- 软件：Node v24.16 ✅（Folia 要求 ≥24）、pnpm 11.7 ✅、npm 11.13 ✅
- **内存注意**：本机常年有其他项目的 3 个 pnpm dev 服务在跑，空闲内存仅 ~1.5GB。跑本项目的双服务 + Electron（约需 4-5GB）前必须关掉无关应用/服务。
- Key 位置（**本机专用，切勿写入仓库**）：本机接口文档目录下的 `music-agent.env.local`（含 DEEPSEEK_API_KEY、SUNO_API_KEY）；仓库 `.gitignore` 已排除 `**/.env.local` 与 `music-agent/data/`。
- Ollama + qwen2.5vl:3b（视觉桥）：**跑链路不需要**，仅开发期核对 UI 渲染的可选辅助；本机 4060 跑它很轻松（模型 ~2GB）。开发会话有视觉能力时直接用截图，不需要它。
- 网络：GitHub 用 HTTPS；真实生成走公司音乐代理（musicproxy，地址通过 `MUSIC_PROXY_BASE_URL` 配置，鉴权头为原始 `Authorization: <MUSIC_PROXY_API_KEY>`）；sunoapi 作为备用第三方 API。

## 八、下一步怎么继续（执行顺序）

1. 启动 Folia 桌面端并开启 Stage Mode（**需要用户在 Folia UI 操作**），完成一次 mock 或真实歌曲的自动推送/自动播放目视验证。
2. 用 `SUNO_PROVIDER=musicproxy` 已生成的真实歌曲，在 Folia 中核对日文副字幕、封面，并分别导出横屏与竖屏视频。
3. Task 10 的用户目视测试与真实横竖屏导出。
4. Task 11 流程文档已按当前实现复查，待最终验收后收口。
5. 全部完成后合并回 main 并推送。

**需要用户亲自操作的步骤**（执行者无法代办）：Folia Stage Mode 开关与 token 复制（Task 1）、浏览器聊天体验测试（Task 9/10）、最终产品测试（Task 10 收尾）。

## 九、关键文件地图

```
docs/
  superpowers/specs/2026-08-21-music-agent-folia-design.md   # 设计（冻结）
  superpowers/plans/2026-08-21-music-agent-folia-design.md   # 实现计划（冻结，1180 行）
  项目执行手册.md                                             # 本文件
music-agent/
  .env.local                        # 本机密钥（已 gitignore）
  src/lib/agent/pi.ts               # pi Agent：6 个音乐工具 + 会话管理 + 付费预算
  src/lib/agent/prompt.ts           # harness 组装（domain/*.md 拼成 SYSTEM_PROMPT）
  src/lib/harness/                  # 领域知识库（写词规范/风格标签/国风/质量门禁/工作流）
  src/lib/audio/lrc.ts              # 歌词数据模型（Task 2 扩展点）
  src/lib/media-output.ts           # 落盘 audio/lrc/meta（Task 3/6 扩展点）
  src/lib/song-delivery.ts          # ensureLocalSong + queueAutoDelivery（自动推送已存在）
  src/lib/folia-stage.ts            # Stage health/push（Task 5 扩展点）
  src/lib/mp3-metadata.ts           # Task 5 新增：USLT 双帧嵌入
  src/lib/cover.ts                  # Task 6 扩展点：renderCoverPng
  src/app/api/jobs/[id]/route.ts    # job 轮询完成回调（tLrc 生成挂钩点，Task 3）
  src/app/api/songs/[id]/push-folia/route.ts  # 手动推送按钮后端
folia-major/（AGPL 上游副本，只改 3 处：）
  electron/stageApi.cjs             # Stage 服务器（Task 7：nextSession 带出 translationLyrics）
  src/types.ts                      # StageMediaSession（Task 7：加字段）
  src/hooks/useStagePlaybackController.ts  # 渲染端消费（Task 7：tLrcContent）
```

## 十、纪律（任何执行者必须遵守）

1. **密钥永不入库**：.env.local / data/ 已 gitignore；任何文档只写 key 的**位置**不写明文。
2. **Folia 最小改动**：上游 AGPL-3.0；改动集中记录（git log），新改动须同步 `test/manual/stage-client/API_SCHEMA.md`。
3. 提交信息与注释用中文；music-agent 用 pnpm、folia-major 用 npm。
4. 实现严格按计划文档走（每任务有测试与提交点）；偏离计划先改计划再执行。
5. mock（WAV）不能验翻译副字幕——这条约束写进了计划 Global Constraints，不要误判为 bug。
